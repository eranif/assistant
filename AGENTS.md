## ROLE

You are a C++ expert developer.

## How to use this file

- Read this file first before doing any work in the repository.
- Treat this file as the primary repository guide.
- Follow the navigation pointers below to find the right source files and docs.
- If `.agents/summary/index.md` exists, use it next for detailed repository context.
- If source code and documentation conflict, trust the source code and note the discrepancy.

## Project navigation

- Core: `assistant/`
- Providers: `assistant/client/`
- MCP: `assistant/mcp.*`, `assistant/cpp-mcp/`
- Demo: `cli/main.cpp`
- Tests: `tests/`
- Docs: `.agents/summary/`

## How AI assistants should use `.agents/summary/`

- Start with `.agents/summary/index.md`.
- Load only the next-most-relevant file.
- `codebase_info.md`: repo map/stack
- `architecture.md`: design/boundaries
- `components.md`: module responsibilities
- `interfaces.md`: APIs/callbacks/integration points
- `data_models.md`: config/state/data structures
- `workflows.md`: runtime sequences
- `dependencies.md`: external/platform deps
- `review_notes.md`: gaps/caveats
- If docs and source conflict, trust source.

## Build

Build:

```bash
mkdir -p .build-debug && cd .build-debug && cmake .. -DCMAKE_BUILD_TYPE=Debug -DENABLE_TESTS=ON
cd .build-debug && make -j10
mkdir -p .build-release && cd .build-release && cmake .. -DCMAKE_BUILD_TYPE=Release -DENABLE_TESTS=ON
cd .build-release && make -j10
```

## Unit Tests

Run tests:

- **Debug mode:** `.build-debug/tests/<TEST_NAME>.exe`
- **Release mode:** `.build-release/tests/<TEST_NAME>.exe`

## Custom Instructions

<!-- This section is maintained by developers and agents during day-to-day work.
     It is NOT auto-generated by codebase-summary and MUST be preserved during refreshes.
     Add project-specific conventions, gotchas, and workflow requirements here. -->
